//
// Created by seungsu on 1/10/23.
//

#include <bits/stdc++.h>
using namespace std;

static int T, R, C;
static vector<string> arr;
static vector<vector<bool>> visited;
static int dx[] = { -1, 1, 0, 0 };
static int dy[] = { 0, 0, -1, 1 };

static int bfs() {

}

void solution1() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> T;

    for (int i = 1; i <= T; i++) {

    }
}